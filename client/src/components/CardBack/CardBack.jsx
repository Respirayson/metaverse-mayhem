import React from 'react'
import styles from '../Card/Card.module.css'

const CardBack = () => {

    return (
        <div className={`${styles.Card} ${styles.CardOpponent}`} />
    )
}

export default CardBack